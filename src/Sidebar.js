import React from 'react';
import Button from './Button';

const Sidebar = () => {
    return (
        <div>
            <Button>ThemeButton</Button>
        </div>
    );
};

export default Sidebar;